import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#0080ff',
    },
    secondary: {
      main: '#9c27b0',
    },
  },
});

export default theme;